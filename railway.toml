[build]
builder = "nixpacks"
buildCommand = "npm install --production --legacy-peer-deps"
# pre-built dist included in repo - no frontend build needed

[deploy]
startCommand = "node server/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
